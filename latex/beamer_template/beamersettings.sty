\ProvidesPackage{beamersettings}

\usepackage{../settings}

\usepackage{csquotes}
\usepackage[
    backend=biber,
    bibwarn=true,
    bibencoding=utf8,
    % set alphabetical order of references for german languages
    % sortlocale=de_DE,
    style=numbering,
]{biblatex}

\usepackage[hidelinks]{hyperref}